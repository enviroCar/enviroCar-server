{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": [ "sensor" ],
    "properties": {
        "sensor": {
            "type": "string",
            "comment": "the name of an existing sensor resource"
        }
    }
}
